import type { PageMeta } from '@/types/page'

const meta: PageMeta = {
  name: 'HTML → Markdown',
  description:
    'Enter a website URL and convert its content to Markdown — preview and download right in your browser',
  author: 'Copilot',
  category: 'tool',
}

export default meta
